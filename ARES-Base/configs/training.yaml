# ARES-Base v1.0 Training Configuration

training:
  gradient_accumulation_steps: 4
  max_grad_norm: 1.0

optimizer:
  weight_decay: 0.1

logging:
  log_interval: 10
  eval_interval: 500
  save_interval: 1000
